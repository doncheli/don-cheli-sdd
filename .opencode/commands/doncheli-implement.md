---
description: Execute TDD tasks — RED → GREEN → REFACTOR
agent: doncheli
---
Execute the TDD implementation cycle. For each task: write test first (RED), then minimal code to pass (GREEN), then refactor. No code without tests.

$ARGUMENTS
