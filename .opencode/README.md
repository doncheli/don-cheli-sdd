# Don Cheli — OpenCode Configuration

## Skills
Don Cheli skills are loaded automatically from `.agent/skills/` via OpenCode's compatibility layer.

27 skills available — run `@doncheli` to see all commands.

## Agent
The main Don Cheli agent is at `.opencode/agents/doncheli.md`.
Invoke with: `@doncheli <command> [arguments]`

## Rules
Rules are loaded from `CLAUDE.md` (OpenCode reads it as fallback) and `AGENTS.md`.
